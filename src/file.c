// 流
// 文本流\r\n | \n 二进制流

// c 语言  输出缓冲区  输入缓冲区
// 按字符 fputc
// 按文本行 fgetc
// 按二进制  fread fwriter('./test.txx', 'wb')

// struct Hear temp[4];
// fread(&temp, iszeof(struct Hero), 4, f_read);

// if(feof(f_red)) {break;}

// 二进制 不要存指针到文件，要把指针内容存到文件